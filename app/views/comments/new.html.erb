<!DOCTYPE html>
<html>
<head>
  <title>BlogApp</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag "/users/comments.css" %>
  <!-- Include Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
  <div class="container">
    <div class="row mt-5">
      <div class="col-md-8 offset-md-2">
        <%= form_with model: @comment, url: user_post_comments_path(@user, @post), method: :post, class: "comments_form" do |f| %>
          <div class="mb-3">
            <%= f.label :text, class: "form-label" %>
            <%= f.text_area :text, class: "form-control", placeholder: "Enter the comment text" %>
          </div>
          <div class="d-grid">
            <%= f.submit "Create Comment", class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</body>
</html>
